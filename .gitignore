# --- DUA: raw MIMIC-IV-ED data must never be committed ---
mimic-iv-ed-2.2/
data_prep/*.csv
data_prep/*.csv.gz
*.csv
*.csv.gz

# model binary: was excluded as "regenerable from code, not committed", but is now
# committed (artifacts/flagger.joblib, artifacts/thresholds.json) as of the public
# deploy — explicit sign-off given that a fitted model's coefficients/TF-IDF
# vocabulary are a derived aggregate, not a raw MIMIC record; see CLAUDE.md DUA
# section. Small aggregate eval numbers derived from it were already committed.

# secrets
.env

# per-case Gemini generation cache (regenerable, not meant to be committed)
data_prep/.case_cache/

# python
__pycache__/
*.pyc
.pytest_cache/

# Claude Code session state — not project content
.claude/
